import { MarginDirective } from './margin.directive';

describe('MarginDirective', () => {
  it('should create an instance', () => {
    const directive = new MarginDirective();
    expect(directive).toBeTruthy();
  });
});
