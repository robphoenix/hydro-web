import { FormatSqlPipe } from './format-sql.pipe';

describe('FormatSqlPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSqlPipe();
    expect(pipe).toBeTruthy();
  });
});
